# /etc/anacrontab: configuration file for anacron

# See anacron(8) and anacrontab(5) for details.

SHELL=/bin/sh
PATH=/sbin:/bin:/usr/sbin:/usr/bin
# the maximal random delay added to the base delay of the jobs
RANDOM_DELAY=45
# the jobs will be started during the following hours only
START_HOURS_RANGE=0-23
HOME=/home/julian

#period in days   delay in minutes   job-identifier   command
@daily 0 generate-wallpapers source $HOME/.zshrc; $HOME/.scripts/convert_wallpapers.sh
@daily 0 empty-trash source $HOME/.zshrc; trash-empty 30
@hourly 0 check-updates source $HOME/.zshrc; $HOME/.scripts/updates-checker/check-updates.sh
